<tr>
  <td><%= test.title %></td>
  <td><%= test.level %></td>
  <td><%= test.questions.count %></td>
  <td><%= link_to t('common.button.open'), admin_test_path(test) %></td>
  <td><%= link_to t('common.button.update'), edit_admin_test_path(test) %></td>
  <td><%= link_to t('common.button.delete'), admin_test_path(test),
                                             method: :delete,
                                             data: { confirm: t('common.pop_up.delete') } %>
  </td>
</tr>
