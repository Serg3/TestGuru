  <h1>Question №<%= @question.id %>. <%= @question.body %></h1>
  <% @answers.each.with_index(1) do |answer, index| %>
    <p><%= index %>. <%= answer.body %></p>
  <% end %>

<%= link_to 'Edit', edit_question_path(@question) %> |
<%= link_to 'Back', @question %>
